import React from 'react';
import PropTypes from 'prop-types';

const Timer = () => {
  

  return (
    <div>
      <h1></h1>
      <button>Start</button>
    </div>
  );


};

Timer.propTypes = {};

export default Timer;